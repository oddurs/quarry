3000 — pid 13000
  process   next-server (v16.3.4)
  verdict   Next.js (web)

  → Next.js                      40  process "next-server"
    AdGuard Home                 20  port 3000  (port alone — not enough to name it)
    Aerospike                    20  port 3000  (port alone — not enough to name it)
    Athens                       20  port 3000  (port alone — not enough to name it)
    Browsersync                  20  port 3000  (port alone — not enough to name it)
    …and 24 more matching the port alone

