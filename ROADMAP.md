# Roadmap

<!-- generated by cairn; edit the files in `cairn/items` and run `cairn render` -->

## v0.1 — First usable version

`##########` 100% · 1 of 1 done · due 2026-12-01

Enough to dogfood in a real project.

### done

- [x] `0004` Adopt cairn for the roadmap <sup>feature</sup>

## v0.2 — Themes and configuration

`##########` 100% · 13 of 13 done

quarry currently hardcodes a Tokyo Night palette in RGB, which overrides

### done

- [x] `0006` Replace the hardcoded palette with colour roles <sup>feature · p0 · theme</sup>
- [x] `0007` Inherit the terminal's palette by default <sup>feature · p0 · theme</sup>
- [x] `0008` Theme files in TOML, with the built-ins compiled in <sup>feature · p1 · theme</sup>
- [x] `0009` Decide how to tell a light terminal from a dark one <sup>spike · p1 · theme</sup>
- [x] `0010` Wear the terminal's own theme by reading Ghostty's theme files <sup>feature · p1 · theme</sup>
- [x] `0011` Read a config file <sup>feature · p0 · config</sup>
- [x] `0012` quarry config — show what is actually in effect <sup>feature · p1 · cli</sup>
- [x] `0013` Make classification configurable <sup>feature · p1 · config</sup>
- [x] `0014` Make the keys configurable <sup>feature · p2 · config</sup>
- [x] `0015` Respect NO_COLOR and a non-colour terminal <sup>feature · p2 · theme</sup>
- [x] `0016` Live theme reload <sup>feature · p3 · theme</sup>
- [x] `0017` Keep the tests honest once colours are data <sup>chore · p0 · testing</sup>
- [x] `0018` Document theming and configuration <sup>docs · p1 · docs</sup>

## v0.3 — Make a scan cheap enough to ignore

`##########` 100% · 6 of 6 done

quarry sits open. A scan every six seconds that costs 111ms is a tool you

### done

- [x] `0020` Read listening sockets from the kernel instead of lsof <sup>feature · p0 · discovery</sup>
- [x] `0021` Stop rebuilding every group on every probe result <sup>feature · p0 · chrome</sup>
- [x] `0022` Draw the rows on screen, not the rows on the machine <sup>feature · p1 · chrome</sup>
- [x] `0023` Stop redrawing ten times a second while nothing happens <sup>feature · p1 · runtime</sup>
- [x] `0024` Stop allocating on every keystroke and every probe <sup>chore · p2 · chrome</sup>
- [x] `0025` Budget the hot paths so a regression fails CI <sup>chore · p0 · testing</sup>

## v0.4 — First-class support for every kind of server

`######····` 58% · 11 of 19 done

quarry sees listening TCP sockets, guesses what they are from a port table and a

### backlog

- [ ] `0034` Speak the protocols that will not introduce themselves <sup>feature · p1 · probe</sup>
- [ ] `0035` Tell a protected service from a broken one <sup>feature · p1 · probe</sup>
- [ ] `0037` Read listening sockets from /proc on Linux <sup>feature · p1 · discovery</sup>
- [ ] `0038` Recognise gRPC and HTTP/2 services <sup>feature · p2 · probe</sup>
- [ ] `0039` Show a tunnel's public URL, not its local port <sup>feature · p2 · attribution</sup>
- [ ] `0040` Group the servers one command started <sup>feature · p2 · attribution</sup>
- [ ] `0042` Show what the TLS certificate says <sup>feature · p2 · probe</sup>
- [ ] `0045` Prove the taxonomy against a real stack <sup>chore · p1 · testing</sup>

### done

- [x] `0027` Make service signatures data instead of a match arm <sup>feature · p0 · discovery</sup>
- [x] `0028` See unix domain sockets <sup>feature · p0 · discovery</sup>
- [x] `0029` Listen to what a service says when you connect <sup>feature · p0 · probe</sup>
- [x] `0030` Attribute a container's ports to the container <sup>feature · p0 · attribution</sup>
- [x] `0031` Decide the probe budget once banners and handshakes are in play <sup>spike · p0 · probe</sup>
- [x] `0032` See UDP listeners <sup>feature · p1 · discovery</sup>
- [x] `0033` Widen the taxonomy to the things people actually run <sup>feature · p1 · discovery</sup>
- [x] `0036` Ask the health endpoint a service actually has <sup>feature · p1 · probe</sup>
- [x] `0041` Copy something you can actually paste <sup>feature · p2 · chrome</sup>
- [x] `0043` Decide how far to chase container runtimes beyond Docker <sup>spike · p2 · attribution</sup>
- [x] `0044` Let people teach quarry about their own servers <sup>docs · p1 · docs</sup>

